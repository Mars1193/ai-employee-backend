# File: app/schemas.pyfrom pydantic import BaseModel, EmailStrfrom typing import List# Schemas for Documentsclass DocumentBase(BaseModel):    filename: strclass Document(DocumentBase):    id: int    owner_id: int    class Config:        orm_mode = True# Schemas for Usersclass UserBase(BaseModel):    email: EmailStrclass UserCreate(UserBase):    password: strclass User(UserBase):    id: int    documents: List[Document] = []    class Config:        orm_mode = True# Schemas for Tokenclass Token(BaseModel):    access_token: str    token_type: strclass TokenData(BaseModel):    email: EmailStr | None = None